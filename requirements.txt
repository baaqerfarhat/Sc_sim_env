# Versions this study was actually run and reported with.
numpy==1.21.5
scipy==1.8.0
osqp==1.1.3
scs==3.3.1
torch==2.14.0+cpu
matplotlib==3.5.1

# resizes already-loaded BLAS/OpenMP pools at runtime. Without it, forked workers
# inherit the parent's thread pools, ignore OMP_NUM_THREADS, and spend most of their
# CPU busy-waiting instead of simulating.
threadpoolctl==3.7.0
